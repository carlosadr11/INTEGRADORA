<p id="notice"><%= notice %></p>
<div class="container">
  <h1> Lista de Pagos</h1>
  <div class="row">
    <div class="col-md-6">
    <div class="table-responsive">
      <table class="table table-bordered">
        <thead>
        <tr>
          <th>Proveedor</th>
          <th>Abono</th>
          <th>Fecha</th>
        </tr>
        <tr>
          <td><%= @payment.provider.nombre  %></td>
          <td><%= @payment.abono %></td>
          <td><%= @payment.fecha %></td>

        </tr>
       </thead>

      </table>
      <%= link_to 'Editar', edit_payment_path(@payment), class: "btn btn-info" %> |
      <%= link_to 'Regresar', payments_path, class: "btn btn-warning" %>
    </div>
   </div>
</div>


</div>