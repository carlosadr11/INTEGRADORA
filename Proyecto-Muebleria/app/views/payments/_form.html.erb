<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="well">
<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
        <div id="error_explanation" xmlns="http://www.w3.org/1999/html">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <div class="field">
        <%= f.label :proveedor %><br>
        <%= f.collection_select(:provider_id, Provider.order(:nombre),:id, :nombre , {prompt: "--Selecciona una opción--" }, :class => 'form-control') %>

      </div>
    </div>

    <div class="form-group">
        <div class="field">

          <%= f.label :abono %><br>
          <div class="input-group">
            <div class="input-group-addon">$</div>
          <%= f.number_field :abono, :class => 'form-control', :placeholder => "Pago" %>
            <div class="input-group-addon">.00</div>
          </div>
        </div>
    </div>

    <div class="form-group">
        <div class="field">
          <%= f.label :fecha %><br>
          <%= f.date_select :fecha, :class => 'form-control' %>
        </div>
    </div>
        <div class="actions">
          <%= f.submit @accion, class: "btn btn-success" %>
          <%= link_to 'Regresar', payments_path, class: "btn btn-info" %>

        </div>

<% end %>
</div>
      </div>
    </div>
  </div>